<!--
  Built on Eleventy Excellent (https://github.com/madrilene/eleventy-excellent)
  by Lene Saile -- credited here in source rather than on-page, since an
  on-page "made with" line would break the single-brand register the spec
  calls for across every page (S1: "no shift to a more corporate register").
  Keep this comment or move the credit into the repo README -- just don't
  drop it entirely; it's due per the starter's license.
-->
<footer class="site-footer mt-l-xl p-s-m">
  <div class="wrapper">
    <div class="flow gutter-xs text-step-min-1">
      <p class="font-display">
        <a href="/" {{ indicateActiveHome }}>{{ meta.siteName }}</a>
        &mdash; <span class="text-theme-text-accent">{{ meta.motto }}</span>
      </p>

      {#- Spec S8 flags this line as optional / "your call whether to keep it".
         Currently ON. Delete this <p> to remove it. #}
      <p class="text-theme-text-accent">This site contains no classified or controlled technical data.</p>

      <p class="text-theme-text-accent">
        &copy; {% year %} {{ meta.siteName }}
      </p>
    </div>
  </div>
</footer>
